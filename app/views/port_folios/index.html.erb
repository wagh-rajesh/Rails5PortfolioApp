<div class="album text-muted">
	<div class="container">
		<div class="row">

			<%#= link_to "Create new Item", new_port_folio_path if logged_in?(:site_admin) %>

			<%= render partial: 'portfolio_item', collection: @portfolio_items %>
		</div>
	</div>
</div>
			